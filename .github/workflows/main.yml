# .github/workflows/main.yml
name: Hourly Data Collection and Model Training

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      OW_API_KEY: ${{ secrets.OW_API_KEY }}
      WAQI_TOKEN: ${{ secrets.WAQI_TOKEN }}
      
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Collect data
        # FIX: Changed to the correct filename
        run: python collect_data.py

      - name: Commit and push changes
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions-bot@github.com"
          
          # Make sure this matches the output file from your script
          git add delhi_aqi_data_waqi.csv
          
          if git diff --staged --quiet; then
            echo "No new data to commit."
          else
            git commit -m "Automated WAQI data collection and model training"
            git push
          fi
